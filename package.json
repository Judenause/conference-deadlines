{
  "name": "conference-deadline-platform",
  "private": true,
  "packageManager": "bun@1.3.14",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "bun run --parallel --filter '*' dev",
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "lint": "biome check apps/api/src apps/mattermost-bot/src apps/mattermost-bot/wrangler.jsonc apps/web/src apps/web/e2e apps/web/index.html apps/web/playwright.config.ts apps/web/vite.config.ts packages scripts fixtures data package.json tsconfig.json tsconfig.base.json biome.json",
    "test": "bun test scripts packages apps/api apps/mattermost-bot && bun --filter @conf/web test",
    "build": "bun scripts/verify-design-contract.ts && bun --filter @conf/web build",
    "build:pages": "bun scripts/verify-design-contract.ts && bun --filter @conf/web build:pages",
    "doctor": "bun run typecheck && bun run lint && bun run test && bun run build",
    "e2e": "bun --filter @conf/web e2e"
  },
  "devDependencies": {
    "@biomejs/biome": "2.2.0",
    "@types/bun": "1.2.19",
    "typescript": "5.9.2"
  }
}
