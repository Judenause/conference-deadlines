# These values are public web configuration, not service-account secrets.
# Configure the same names as GitHub Actions repository variables before Pages deploys.
VITE_FIREBASE_API_KEY=
VITE_FIREBASE_PROJECT_ID=
VITE_GOOGLE_CLIENT_ID=
